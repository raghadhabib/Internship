import { HighlightCompleted } from './highlight-completed';

describe('HighlightCompleted', () => {
  it('should create an instance', () => {
    const directive = new HighlightCompleted();
    expect(directive).toBeTruthy();
  });
});
